pre-commit:
  parallel: false
  commands:
    fmt:
      glob: "src/**/*.rs"
      run: cargo fmt
    lint:
      glob: "src/**/*.rs"
      run: cargo clippy --fix --allow-dirty --allow-staged -- -D warnings
